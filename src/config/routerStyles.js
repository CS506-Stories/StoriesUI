import { StyleSheet } from 'react-native';

const routerStyles = StyleSheet.create({
  leftButtonStyle: {
    paddingLeft: 16,
  },
  rightButtonStyle: {
    paddingRight: 16,
  },
});

export default routerStyles;
